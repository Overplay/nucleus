/**
 *
 *
 * If sails.config.theme.themeName is not undefined, then it is applied to the end of ejs files
 * like so:  login.ejs becomes login-[themeName].ejs
 *
 */

module.exports.theme = {
    themeName: undefined
};